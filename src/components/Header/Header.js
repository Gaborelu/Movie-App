//Voicu Diana
import React from 'react'
import './Header.css'

const Header = () => {
  return (
    <header className='header' onClick={() => window.scroll(0, 0)}>
      React Movie App
    </header>
  )
}

export default Header
